apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      deployment: kuadrant-qe-pipeline
      app: test-disconnected

components:
  - ../../../tasks/login/
  - ../../../tasks/test/
  - ../../../tasks/disconnected/

resources:
  - pipeline.yaml
